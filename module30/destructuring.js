const heros = ['sakib', 'bappi', 'zakir'];
const [king, lost, notun] = heros;
console.log (notun);

const student = {
    name: 'zakir',
    age: 29,
    nationality: 'Bangladesh'
}
const {name, nationality, age} = student;
 console.log (`${name} from ${nationality} and his age: ${age}`);